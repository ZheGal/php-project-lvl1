#!/usr/bin/env php
<?php
require_once("vendor/autoload.php");

use function cli\line;
use function cli\prompt;
use Brain\Games\Cli;
	
line('Welcome to the Brain Games!');
$name = prompt('May I have your name?');

$user = new Brain\Games\Cli($name);

line("Hello, %s!", $name);